"use client";
import {useEffect,useMemo,useState} from 'react';import DmsShell from '../../../DmsShell';import FinanceNav from '../FinanceNav';import {createClient} from '../../../../lib/supabase/client';import {getCloudContext} from '../../../../lib/dealercore/cloud';
const money=(n:number)=>new Intl.NumberFormat('en-GB',{style:'currency',currency:'GBP'}).format(n||0);
export default function Ledger(){const[coa,setCoa]=useState<any[]>([]),[journals,setJournals]=useState<any[]>([]),[loading,setLoading]=useState(true);async function load(){setLoading(true);try{await getCloudContext();const s=createClient();const[a,b]=await Promise.all([s.from('chart_of_accounts').select('*').order('code'),s.from('journal_entries').select('*,journal_lines(*)').order('journal_date',{ascending:false})]);if(a.error)throw a.error;if(b.error)throw b.error;setCoa(a.data||[]);setJournals(b.data||[])}catch(e:any){alert(e.message)}finally{setLoading(false)}}useEffect(()=>{load()},[]);const totals=useMemo(()=>journals.reduce((a,j)=>{for(const l of j.journal_lines||[]){a.debit+=(+l.debit||0);a.credit+=(+l.credit||0)}return a},{debit:0,credit:0}),[journals]);async function manualJournal(){const debitCode=prompt('Debit nominal code','');if(!debitCode)return;const creditCode=prompt('Credit nominal code','');if(!creditCode)return;const debit=coa.find(x=>x.code===debitCode),credit=coa.find(x=>x.code===creditCode);if(!debit||!credit)return alert('Nominal code not found.');const amount=Number(prompt('Journal amount','0.00')||0);if(!Number.isFinite(amount)||amount<=0)return alert('Enter a valid amount.');const desc=prompt('Journal description','Manual journal')||'Manual journal';const date=prompt('Journal date',new Date().toISOString().slice(0,10));if(!date)return;try{const ctx=await getCloudContext(),s=createClient();const {data,error}=await s.from('journal_entries').insert({organization_id:ctx.organizationId,depot_id:ctx.depotId,journal_date:date,description:desc,reference:'MANUAL',source_type:'manual',status:'posted',created_by:ctx.userId,posted_by:ctx.userId,posted_at:new Date().toISOString()}).select('id').single();if(error)throw error;const {error:le}=await s.from('journal_lines').insert([{journal_entry_id:data.id,account_id:debit.id,line_no:1,description:desc,debit:amount,credit:0},{journal_entry_id:data.id,account_id:credit.id,line_no:2,description:desc,debit:0,credit:amount}]);if(le)throw le;await load()}catch(e:any){alert(e.message)}}return <DmsShell active="Finance" eyebrow="FINANCE / NOMINAL LEDGER" title="General Ledger" subtitle="Double-entry journals, nominal activity and audit trail" actions={<button className="dms-primary" onClick={manualJournal}>+ Manual journal</button>}><FinanceNav active="General Ledger"/><div className="dms-kpi-grid"><div><span>Journal entries</span><strong>{journals.length}</strong><small>Posted entries</small></div><div><span>Total debits</span><strong>{money(totals.debit)}</strong><small>Ledger movements</small></div><div><span>Total credits</span><strong>{money(totals.credit)}</strong><small>Ledger movements</small></div><div><span>Control difference</span><strong>{money(totals.debit-totals.credit)}</strong><small>Should remain £0.00</small></div></div>{loading?<div className="dms-empty">Loading general ledger…</div>:<section className="dms-panel"><div className="finance-table-head ledger"><span>Date</span><span>Reference</span><span>Description</span><span>Source</span><span>Debit</span><span>Credit</span><span>Status</span></div>{journals.map(j=>{const d=(j.journal_lines||[]).reduce((s:number,l:any)=>s+(+l.debit||0),0),c=(j.journal_lines||[]).reduce((s:number,l:any)=>s+(+l.credit||0),0);return <div className="finance-table-row ledger" key={j.id}><span>{j.journal_date}</span><span>{j.reference||'—'}</span><span><b>{j.description}</b><small>{(j.journal_lines||[]).length} lines</small></span><span>{j.source_type||'manual'}</span><span>{money(d)}</span><span>{money(c)}</span><span>{j.status||'posted'}</span></div>})}</section>}</DmsShell>}
