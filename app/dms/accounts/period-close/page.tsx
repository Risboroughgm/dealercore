"use client";
import {useEffect,useState} from 'react';import DmsShell from '../../../DmsShell';import FinanceNav from '../FinanceNav';import {createClient} from '../../../../lib/supabase/client';import {getCloudContext} from '../../../../lib/dealercore/cloud';const defaultTasks=[['BANK','Reconcile all bank accounts'],['AR','Review aged receivables and bad debts'],['AP','Review aged payables and unapproved bills'],['VAT','Reconcile VAT control account'],['STOCK','Review stock valuation and adjustments'],['FA','Post depreciation and asset disposals'],['ACCRUALS','Post accruals and prepayments'],['TB','Review trial balance and suspense'],['MGMT','Approve management accounts']];
export default function PeriodClose(){const[periods,setPeriods]=useState<any[]>([]),[tasks,setTasks]=useState<any[]>([]);async function load(){try{await getCloudContext();const s=createClient();const[a,b]=await Promise.all([s.from('accounting_periods').select('*').order('start_date',{ascending:false}),s.from('period_close_tasks').select('*').order('task_code')]);if(a.error)throw a.error;if(b.error)throw b.error;setPeriods(a.data||[]);setTasks(b.data||[])}catch(e:any){alert(e.message)}}useEffect(()=>{load()},[]);async function createPeriod(){const name=prompt('Period name','Month End');if(!name)return;const start=prompt('Start date',new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10));if(!start)return;const end=prompt('End date',new Date().toISOString().slice(0,10));if(!end)return;try{const ctx=await getCloudContext(),s=createClient();const{data,error}=await s.from('accounting_periods').insert({organization_id:ctx.organizationId,name,start_date:start,end_date:end,status:'open'}).select('id').single();if(error)throw error;const{error:te}=await s.from('period_close_tasks').insert(defaultTasks.map(([code,task])=>({organization_id:ctx.organizationId,period_id:data.id,task_code:code,task_name:task,status:'open'})));if(te)throw te;load()}catch(e:any){alert(e.message)}}async function toggle(t:any){try{const ctx=await getCloudContext(),s=createClient(),complete=t.status!=='complete';const{error}=await s.from('period_close_tasks').update({status:complete?'complete':'open',completed_at:complete?new Date().toISOString():null,completed_by:complete?ctx.userId:null}).eq('id',t.id);if(error)throw error;load()}catch(e:any){alert(e.message)}}return <DmsShell active="Finance" eyebrow="FINANCE / PERIOD CONTROL" title="Period Close" subtitle="Month-end and year-end close checklist, ownership and lock control" actions={<button className="dms-primary" onClick={createPeriod}>+ Close period</button>}><FinanceNav active="Period Close"/><div className="period-close-grid">{periods.map(p=>{const xs=tasks.filter(t=>t.period_id===p.id),done=xs.filter(t=>t.status==='complete').length;return <section className="dms-panel" key={p.id}><div className="dms-panel-head"><div><h2>{p.name}</h2><span>{p.start_date} to {p.end_date} · {p.status}</span></div><b>{done}/{xs.length}</b></div><div className="period-task-list">{xs.map(t=><button key={t.id} onClick={()=>toggle(t)} className={t.status==='complete'?'complete':''}><span>{t.status==='complete'?'✓':'○'}</span><div><b>{t.task_name}</b><small>{t.task_code}</small></div></button>)}{!xs.length&&<div className="dms-empty">No close tasks configured.</div>}</div></section>})}{!periods.length&&<div className="dms-empty">Create your first month-end or year-end close period.</div>}</div></DmsShell>}
