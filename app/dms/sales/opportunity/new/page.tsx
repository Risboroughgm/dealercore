"use client";
import {useEffect,useState} from "react";
import {useRouter,useSearchParams} from "next/navigation";
import DmsShell from "../../../../DmsShell";
const K="dealercore-v2",OP="dealercore-opportunities-v1";
export default function NewOpportunity(){const router=useRouter(),sp=useSearchParams();const preset=sp.get("customerId")||"";const [customers,setCustomers]=useState<any[]>([]),[customerId,setCustomerId]=useState(preset);useEffect(()=>{try{const c=JSON.parse(localStorage.getItem(K)||"{}");setCustomers(c.customers||[])}catch{}},[]);function save(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const opps=JSON.parse(localStorage.getItem(OP)||"[]");opps.unshift({id:crypto.randomUUID(),customerId,title:String(f.get("title")),stage:"Lead",value:Number(f.get("value")||0),probability:Number(f.get("probability")||25),owner:String(f.get("owner")||"Jonathan Quinton"),source:String(f.get("source")||"Existing customer"),closeDate:String(f.get("closeDate")||""),notes:String(f.get("notes")||"")});localStorage.setItem(OP,JSON.stringify(opps));router.push(customerId?`/dms/customers/${customerId}`:"/dms/sales")}return <DmsShell active="Sales" eyebrow="SALES / NEW OPPORTUNITY" title="Create Opportunity" subtitle="Start a connected dealership sales lifecycle"><form className="dms-form-grid" onSubmit={save}><section className="dms-panel dms-form-panel"><h2>Customer & opportunity</h2><label><span>Customer account</span><select value={customerId} onChange={e=>setCustomerId(e.target.value)} required><option value="">Select customer</option>{customers.map(c=><option key={c.id} value={c.id}>{c.company||c.name}</option>)}</select></label><label><span>Opportunity title</span><input name="title" required placeholder="e.g. Commercial robotic mower installation"/></label><div className="dms-form-two"><label><span>Estimated value ex VAT</span><input name="value" type="number" min="0" step="0.01" required/></label><label><span>Probability %</span><input name="probability" type="number" min="0" max="100" defaultValue="25"/></label></div><label><span>Expected close date</span><input name="closeDate" type="date"/></label></section><section className="dms-panel dms-form-panel"><h2>Ownership & qualification</h2><label><span>Salesperson / owner</span><input name="owner" defaultValue="Jonathan Quinton"/></label><label><span>Lead source</span><select name="source"><option>Existing customer</option><option>Website</option><option>Referral</option><option>Telephone</option><option>Showroom</option><option>Outbound</option><option>Manufacturer lead</option></select></label><label><span>Internal notes</span><textarea name="notes" rows={8} placeholder="Requirement, machine interest, site details, next action..."/></label><button className="dms-primary" type="submit">Create opportunity</button></section></form></DmsShell>}
