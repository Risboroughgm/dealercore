"use client";import Link from "next/link";import {useEffect,useState} from "react";import DmsShell from "../../DmsShell";const K="dealercore-v2",OK="dealercore-enterprise-orders",OP="dealercore-opportunities-v1";const m=(n:number)=>new Intl.NumberFormat("en-GB",{style:"currency",currency:"GBP",maximumFractionDigits:0}).format(n||0);const stages=["Lead","Qualified","Demo / Survey","Quoted","Negotiation","Won","Lost"];export default function Sales(){const [c,setC]=useState<any>({customers:[],quotes:[]}),[o,setO]=useState<any[]>([]),[p,setP]=useState<any[]>([]);useEffect(()=>{try{setC((s:any)=>({...s,...JSON.parse(localStorage.getItem(K)||"{}")}));setO(JSON.parse(localStorage.getItem(OK)||"[]"));setP(JSON.parse(localStorage.getItem(OP)||"[]"))}catch{}},[]);const customer=(id:string)=>c.customers.find((z:any)=>z.id===id);function saveOpps(next:any[]){setP(next);localStorage.setItem(OP,JSON.stringify(next))}function stage(id:string,stage:string){saveOpps(p.map(x=>x.id===id?{...x,stage}:x))}function createQuote(x:any){const latest=JSON.parse(localStorage.getItem(K)||"{}");const quotes=latest.quotes||[];const number=`Q-${new Date().getFullYear()}-${String(quotes.length+1).padStart(4,"0")}`;quotes.unshift({id:crypto.randomUUID(),number,customerId:x.customerId,lines:[{qty:1,unit:+x.value||0,vat:20,description:x.title}],status:"Draft",createdAt:new Date().toISOString(),sourceOpportunityId:x.id});const nextCore={...latest,quotes};localStorage.setItem(K,JSON.stringify(nextCore));setC((v:any)=>({...v,quotes}));stage(x.id,"Quoted")}return <DmsShell active="Sales" eyebrow="SALES CONTROL" title="Sales & Order Management" subtitle="Opportunities, quotations, orders and fulfilment" actions={<><Link className="dms-secondary" href="/dms/sales/opportunity/new">+ Opportunity</Link><Link className="dms-primary" href="/sales-order/new">+ Sales order</Link></>}><div className="dms-kpi-grid"><div><span>Open opportunities</span><strong>{p.filter(x=>!['Won','Lost'].includes(x.stage)).length}</strong><small>{m(p.filter(x=>!['Won','Lost'].includes(x.stage)).reduce((s,x)=>s+(+x.value||0),0))}</small></div><div><span>Live quotations</span><strong>{c.quotes.filter((x:any)=>['Draft','Sent'].includes(x.status)).length}</strong><small>Awaiting progression</small></div><div><span>Sales orders</span><strong>{o.length}</strong><small>{o.filter(x=>!['Delivered','Cancelled'].includes(x.status)).length} open</small></div></div><div className="dms-module-stack"><section className="dms-panel"><div className="dms-panel-head"><div><h2>Opportunity register</h2><span>Progress prospects through the connected sales lifecycle</span></div></div><div className="sales-opportunity-head"><span>Account</span><span>Opportunity</span><span>Stage</span><span>Owner / close</span><span>Value</span><span>Action</span></div>{p.map(x=><div className="sales-opportunity-row" key={x.id}><span><Link href={`/dms/customers/${x.customerId}`}><b>{customer(x.customerId)?.company||customer(x.customerId)?.name||"—"}</b></Link></span><span><b>{x.title}</b><small>{x.source||"No source"}</small></span><span><select value={x.stage} onChange={e=>stage(x.id,e.target.value)}>{stages.map(s=><option key={s}>{s}</option>)}</select></span><span>{x.owner||"—"}<small>{x.closeDate||"No close date"}</small></span><span><b>{m(x.value)}</b><small>{x.probability||0}% probability</small></span><span>{x.stage!=="Quoted"&&!c.quotes.some((q:any)=>q.sourceOpportunityId===x.id)?<button className="dms-table-action" onClick={()=>createQuote(x)}>Create quote</button>:<span className="workflow-complete">Quote created</span>}</span></div>)}{!p.length&&<div className="dms-empty">No opportunities in the current dataset.</div>}</section><section className="dms-panel"><div className="dms-panel-head"><div><h2>Quotation register</h2><span>Draft and issued quotations linked to customer accounts</span></div></div><div className="dms-data-head"><span>Quote</span><span>Customer</span><span>Status</span><span>Created</span><span>Source</span><span>Value</span></div>{c.quotes.map((x:any)=><div className="dms-data-row" key={x.id}><span><b>{x.number}</b></span><span><Link href={`/dms/customers/${x.customerId}`}>{customer(x.customerId)?.company||customer(x.customerId)?.name||"—"}</Link></span><span>{x.status}</span><span>{x.createdAt?.slice(0,10)||"—"}</span><span>{x.sourceOpportunityId?"Opportunity":"Direct"}</span><span><b>{m((x.lines||[]).reduce((s:number,l:any)=>s+(+l.qty||0)*(+l.unit||0)*(1+(+l.vat||0)/100),0))}</b></span></div>)}</section><section className="dms-panel"><div className="dms-panel-head"><div><h2>Sales order register</h2><span>Confirmed orders and fulfilment status</span></div></div><div className="dms-data-head"><span>Order</span><span>Customer</span><span>Status</span><span>Delivery</span><span>Deposit</span><span>Value</span></div>{o.map(x=><div className="dms-data-row" key={x.id}><span><b>{x.number}</b></span><span><Link href={`/dms/customers/${x.customerId}`}>{customer(x.customerId)?.company||customer(x.customerId)?.name||"—"}</Link></span><span>{x.status}</span><span>{x.deliveryDate||"—"}</span><span>{m(x.deposit||0)}</span><span><b>{m(x.value||0)}</b></span></div>)}{!o.length&&<div className="dms-empty">No sales orders in the current dataset.</div>}</section></div></DmsShell>}