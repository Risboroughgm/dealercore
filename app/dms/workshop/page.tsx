"use client";import Link from "next/link";import {useEffect,useState} from "react";import DmsShell from "../../DmsShell";const K="dealercore-v2";const m=(n:number)=>new Intl.NumberFormat("en-GB",{style:"currency",currency:"GBP"}).format(n||0);export default function Workshop(){const [c,setC]=useState<any>({customers:[],workshop:[]});useEffect(()=>{try{setC((s:any)=>({...s,...JSON.parse(localStorage.getItem(K)||"{}")}))}catch{}},[]);return <DmsShell active="Workshop" eyebrow="AFTERSALES OPERATIONS" title="Workshop Control" subtitle="Jobs, technicians, machines, approvals and WIP" actions={<Link className="dms-primary" href="/dms/workshop/new">+ New job</Link>}><div className="dms-kpi-grid">{['Booked','Awaiting approval','Awaiting parts','In progress','Finished'].map(s=><div key={s}><span>{s}</span><strong>{c.workshop.filter((x:any)=>x.status===s).length}</strong><small>workshop jobs</small></div>)}</div><section className="dms-panel"><div className="dms-data-head"><span>Job</span><span>Customer / machine</span><span>Status</span><span>Technician</span><span>Due</span><span>Job value</span></div>{c.workshop.map((x:any)=><div className="dms-data-row" key={x.id}><span><b>{x.number}</b></span><span>{c.customers.find((z:any)=>z.id===x.customerId)?.company||c.customers.find((z:any)=>z.id===x.customerId)?.name||"Customer"}<small>{x.machine||"Machine not set"}</small></span><span>{x.status}</span><span>{x.technician||"Unallocated"}</span><span>{x.dueDate||"—"}</span><span>{m((+x.labourHours||0)*(+x.labourRate||0)+(+x.partsSell||0))}</span></div>)}{!c.workshop.length&&<div className="dms-empty">No workshop jobs in the current dataset.</div>}</section></DmsShell>}