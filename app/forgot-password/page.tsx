"use client";
import Link from "next/link";
import {FormEvent,useState} from "react";
import {createClient} from "../../lib/supabase/client";
export default function ForgotPassword(){const[email,setEmail]=useState("");const[msg,setMsg]=useState("");const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);const supabase=createClient();const{error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/auth/callback?next=/update-password`});setMsg(error?error.message:"If that account exists, a password reset email has been sent.");setBusy(false)}return <main className="auth-shell"><section className="auth-simple"><div className="auth-lockup"><div className="auth-mark"><span>◇</span></div><div><strong>DEALER<em>CORE</em></strong><small>SECURE ACCOUNT RECOVERY</small></div></div><h1>Reset your password</h1><p>Enter your dealership email address and DealerCore will send a secure recovery link.</p><form className="auth-form" onSubmit={submit}><label><span>Email address</span><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><button className="auth-submit" disabled={busy}>{busy?"Sending…":"Send recovery email"}</button></form>{msg&&<div className="auth-message">{msg}</div>}<div className="auth-links"><Link href="/login">← Back to sign in</Link></div></section></main>}
